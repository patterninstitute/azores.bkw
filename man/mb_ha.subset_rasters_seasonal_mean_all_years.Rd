% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bkw.subset_rasters.R
\name{mb_ha.subset_rasters_seasonal_mean_all_years}
\alias{mb_ha.subset_rasters_seasonal_mean_all_years}
\title{Compute seasonal mean rasters across all years}
\usage{
mb_ha.subset_rasters_seasonal_mean_all_years(
  season,
  sst_rast,
  hmlmeso_rast,
  lmeso_rast,
  mlmeso_rast,
  depth_rast,
  slope_rast
)
}
\arguments{
\item{season}{A character string indicating the target season.
Must be one of \code{"JFM"}, \code{"AMJ"}, \code{"JAS"}, or \code{"OND"}.}

\item{sst_rast}{A \code{SpatRaster} containing sea surface temperature layers
named in the format \code{sst_YYYYMM}.}

\item{hmlmeso_rast}{A \code{SpatRaster} containing high mesozooplankton biomass
layers named in the format \code{hmlmeso_YYYYMM}.}

\item{lmeso_rast}{A \code{SpatRaster} containing low mesozooplankton biomass
layers named in the format \code{lmeso_YYYYMM}.}

\item{mlmeso_rast}{A \code{SpatRaster} containing medium–large mesozooplankton
biomass layers named in the format \code{mlmeso_YYYYMM}.}

\item{depth_rast}{A static (non time-dependent) \code{SpatRaster} representing depth.}

\item{slope_rast}{A static (non time-dependent) \code{SpatRaster} representing slope.}
}
\value{
A \code{SpatRaster} composed of:
\itemize{
\item Seasonal mean \code{sst}
\item Seasonal mean \code{hmlmeso}
\item Seasonal mean \code{lmeso}
\item Seasonal mean \code{mlmeso}
\item \code{depth} (static layer)
\item \code{slope} (static layer)
}
}
\description{
\code{\link[=mb_ha.subset_rasters_seasonal_mean_all_years]{mb_ha.subset_rasters_seasonal_mean_all_years()}} extracts all layers
corresponding to a given season across multiple years for each dynamic
predictor and computes the mean raster for that season.

Static predictors (e.g., depth and slope) are appended unchanged.

Seasons are defined as:
\itemize{
\item \code{"JFM"}: January–March
\item \code{"AMJ"}: April–June
\item \code{"JAS"}: July–September
\item \code{"OND"}: October–December
}

This function is intended for generating climatological seasonal means
across years for habitat suitability modelling.
}
\details{
The function internally identifies layers corresponding to the selected
season using regular expressions that match the pattern \code{prefix_YYYYMM}.
All matching layers across years and across the three months defining
the season are averaged using \code{\link[terra:summarize-generics]{terra::mean()}}.

Layer naming must follow the convention \code{prefix_YYYYMM} for correct
matching. An error is returned if an invalid season is supplied.
}
