% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_raster_names.R
\name{update_raster_names}
\alias{update_raster_names}
\title{Update raster layer names and metadata}
\usage{
update_raster_names(rast, prefix, var_name, long_name)
}
\arguments{
\item{rast}{A \code{SpatRaster} object whose layer names and metadata will be updated.}

\item{prefix}{A character string used as a prefix for each raster layer name.
The final layer names will be constructed as \code{prefix_YYYYMMDD}.}

\item{var_name}{A character string defining the variable name to assign to
the raster via \code{terra::varnames()}.}

\item{long_name}{A character string defining the long descriptive name to
assign to the raster via \code{terra::longnames()}.}
}
\value{
A \code{SpatRaster} object with:
\itemize{
\item Layer names formatted as \code{prefix_YYYYMMDD}
\item Variable name set via \code{terra::varnames()}
\item Long descriptive name set via \code{terra::longnames()}
}
}
\description{
\code{\link[=update_raster_names]{update_raster_names()}} updates the layer names, variable names, and long
names of a \code{SpatRaster} object. Layer names are generated by combining a
user-defined prefix with a daily date sequence spanning from 2012-01-01
to 2018-12-31.

This function is useful when working with time-series raster stacks where
each layer represents a daily value and needs standardized naming and
metadata attributes.
}
\details{
The function internally creates a daily date sequence between
2012-01-01 and 2018-12-31 using \code{lubridate::ymd()} and formats it as
\code{YYYYMMDD}. The length of this sequence must match the number of layers
in \code{rast}; otherwise, an error will occur when assigning names.
}
