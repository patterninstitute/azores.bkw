% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bkw.subset_rasters.R
\name{bkw.subset_rasters_monthly_mean_all_years}
\alias{bkw.subset_rasters_monthly_mean_all_years}
\title{Compute monthly mean rasters across all years}
\usage{
bkw.subset_rasters_monthly_mean_all_years(
  month,
  sst_rast,
  hmlmeso_rast,
  lmeso_rast,
  mlmeso_rast,
  depth_rast,
  slope_rast
)
}
\arguments{
\item{month}{A character string indicating the target month in \code{"MM"}
format (e.g., \code{"01"} for January, \code{"12"} for December).}

\item{sst_rast}{A \code{SpatRaster} containing sea surface temperature layers
named in the format \code{sst_YYYYMM}.}

\item{hmlmeso_rast}{A \code{SpatRaster} containing high mesozooplankton biomass
layers named in the format \code{hmlmeso_YYYYMM}.}

\item{lmeso_rast}{A \code{SpatRaster} containing low mesozooplankton biomass
layers named in the format \code{lmeso_YYYYMM}.}

\item{mlmeso_rast}{A \code{SpatRaster} containing medium–large mesozooplankton
biomass layers named in the format \code{mlmeso_YYYYMM}.}

\item{depth_rast}{A static (non time-dependent) \code{SpatRaster} representing depth.}

\item{slope_rast}{A static (non time-dependent) \code{SpatRaster} representing slope.}
}
\value{
A \code{SpatRaster} composed of:
\itemize{
\item Monthly mean \code{sst}
\item Monthly mean \code{hmlmeso}
\item Monthly mean \code{lmeso}
\item Monthly mean \code{mlmeso}
\item \code{depth} (static layer)
\item \code{slope} (static layer)
}
}
\description{
\code{\link[=bkw.subset_rasters_monthly_mean_all_years]{bkw.subset_rasters_monthly_mean_all_years()}} extracts all layers
corresponding to a given month across multiple years for each dynamic
predictor and computes the mean raster for that month.

Static predictors (e.g., depth and slope) are appended unchanged.

This function is intended for generating climatological monthly means
across years for habitat suitability modelling.
}
\details{
The function internally identifies layers corresponding to the selected
month using regular expressions that match the pattern \code{prefix_YYYYMM}.
All matching layers across years are averaged using \code{\link[terra:summarize-generics]{terra::mean()}}.

Layer naming must follow the convention \code{prefix_YYYYMM} for correct
matching. If no layers match the requested month, the function will return
an error.
}
